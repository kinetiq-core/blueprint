node_modules/
*.log
.DS_Store

# dist/ is intentionally committed — shipped to consumers via git dependency.
# Rebuilt by `npm run build` and refreshed before every release.

# Consumer-side derived artefacts (if you run blueprint inside this repo for testing)
docs_public/
generated/
