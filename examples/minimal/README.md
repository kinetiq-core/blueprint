# Minimal Blueprint example

The smallest working setup: one spec in `docs/specs/`, one config.

```bash
cd examples/minimal
# When the full CLI is wired, these will work:
#   blueprint collect
#   blueprint aggregate
#   blueprint generate
```
